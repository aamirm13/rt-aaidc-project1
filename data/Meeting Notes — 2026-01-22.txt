Meeting Notes — 2026-01-22

Decisions
- Use cosine similarity for retrieval.
- Default top_k for retrieval: 5.
- Relevance threshold initial value: 0.80 similarity (or distance <= 0.20 if using cosine distance).

Action Items
- Add unit tests for chunking boundaries.
- Add a "Show sources" toggle in the UI.

Open Questions
- Should we index PDFs in v2?
- Do we need per-document filters (metadata filtering)?
